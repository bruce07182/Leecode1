// Add beginner-friendly explanations of both WHAT Python operations do and WHY they are used here.
(() => {
  const A=window.__learningV3Answers;
  if(!A) return;
  const add=(i,text)=>{ if(A[i]) A[i].explain=text; };

  add(1,'result is a list, so result.append(x) adds another matching value to the end without replacing values already collected. result[x] = ... would mean “replace the item at index x,” which is a different operation and would fail when that index does not exist. Here x is a value from nums, not a result-list position.');
  add(2,'max(nums) asks Python for the largest value in nums. It is the simplest Python form. The deeper version keeps a variable called best and updates it while scanning, which shows how max can be implemented.');
  add(3,'s[start:stop:step] is Python slicing. In s[::-1], start and stop are omitted, so the whole string is used; step = -1 means move backward one character at a time. Example: "abc"[::-1] becomes "cba".');
  add(5,'Counter(nums) is a ready-made frequency dictionary: for [1, 2, 2] it produces counts like {1: 1, 2: 2}. In the manual version, counts.get(x, 0) means “give me counts[x] if x already exists; otherwise give me 0.” We use get because counts[x] alone raises KeyError the first time x appears. Then counts[x] = old_count + 1 stores the new count. This is assignment because a dictionary keeps one current count per key; append would add another item to a list instead of updating the count for x.');
  add(6,'set(nums) keeps one copy of each distinct value. If len(set(nums)) is smaller than len(nums), something appeared more than once. The deeper version uses seen.add(x): a set uses add, not append, because a set is an unordered collection of unique values rather than a list.');
  add(7,'items[::-1] returns a reversed copy. A stack removes the last item first, so reversing the arrival order gives the removal order. The deeper version uses append to push a new item onto the end of the list and pop to remove the last item.');
  add(8,'sorted(pairs, key=lambda pair: pair[1]) returns the pairs ordered by their second value. lambda pair: pair[1] is a tiny function saying “for each pair, use element 1 as the sort key.” We use key because Python otherwise compares the pairs starting from their first value.');
  add(9,'nums[:k] takes the first k values to create the initial window. When the window moves right, += adds the new value entering and -= removes the old value leaving. This is why we update window instead of calling sum on every window again.');
  add(10,'result.append(total) adds each newly computed running total to the end of the output list. We append because every position needs its own answer. Writing result[x] = total would treat x as an index and replace a position rather than grow the result in order.');
  add(11,'// is integer division, so the middle position is an integer index. left + (right - left) // 2 chooses the middle of the current search range. Binary search uses indexes because it repeatedly changes the left and right boundaries.');
  add(12,'deque is Python’s efficient queue type. q.append(value) puts a new item at the back; q.popleft() removes the oldest item from the front. We use append here because a queue grows with new items; assigning q[x] would replace an existing position rather than enqueue something.');
  add(13,'graph is a dictionary whose key is a node and whose value is that node’s neighbor list. graph.get(node, []) means “return graph[node] if node exists; otherwise return an empty neighbor list.” Using graph[node] directly would raise KeyError for a missing node.');
  add(18,'a, b = b, a + b is simultaneous assignment: Python first computes the right side from the old values, then updates both variables. It lets us keep only the two previous DP states instead of storing an entire list.');
  add(20,'enumerate(nums) gives both i (the index) and x (the value). need = target - x calculates the exact partner required. seen[x] = i stores one index for the value x, because later we want to retrieve that index with seen[need]. This is dictionary assignment, not append: x is a key, and its index is the value associated with that key.');
  add(21,'count.get(ch, 0) means “use the current count for ch, or 0 if ch has never appeared.” count[ch] alone would raise KeyError on the first occurrence. count[ch] = ... then updates the one count associated with that character. append would be appropriate for a list of items, but here the dictionary needs one key → count entry per character. enumerate(s) is used in the second pass because the answer is an index.');
  add(22,'pairs is a dictionary from closing bracket to the opening bracket it requires. pairs[ch] retrieves that required opener. stack.append(ch) saves an opening bracket because several unmatched openers may need to be remembered in order. stack.pop() removes the most recent one because bracket matching is last-in-first-out.');
  add(23,'stack.append(x) keeps every pushed value because a stack can contain many items. mins.append(...) keeps a parallel minimum for the same stack depth. We append rather than assign by x because x is the stored value, not an array index, and every push creates a new stack position. mins[-1] means the last/current minimum.');
  add(24,'merged.append(interval[:]) adds a new non-overlapping interval to the output because it represents another result item. But merged[-1][1] = ... uses assignment when intervals overlap because we are updating the end of the existing last interval, not creating another interval. That difference—append for a new result, assignment for changing an existing result—is important here.');
  add(25,'key=lambda x: x[1] sorts intervals by ending time rather than starting time. The greedy strategy wants the compatible interval that ends earliest, because that leaves the most room for later intervals. float("-inf") starts last_end below every normal endpoint so the first interval can be accepted.');
  add(27,'bisect_left(nums, target) returns the leftmost position where target can be inserted while nums stays sorted. If target already exists, it returns its first valid position. The deeper version is useful because it shows how this boundary-style binary search works instead of hiding it in the library call.');
  add(28,'deque([start]) creates the BFS queue with the starting node already inside. seen = {start} is a set containing the start node. When a new neighbor is discovered, seen.add(nxt) records membership while q.append(nxt) schedules it for later processing. They are different structures: the set prevents repeats; the queue controls visit order.');
  add(31,'sorted(nums, reverse=True) orders values from largest to smallest, and [:k] takes the first k. The deeper heap version uses heappush to add each candidate and heappop to discard the smallest whenever more than k are kept, so the heap never needs to hold all n values.');
  add(32,'path.append(nums[i]) makes a choice by adding one value to the current subset. result.append(path[:]) saves a COPY of the current path; without [:], every saved entry would refer to the same list as it later changes. path.pop() then undoes the last choice so another branch can be explored.');
  add(33,'a, b = b, a + b updates both values from the old a and b at the same time. It is used instead of appending every answer to a dp list because the next stair count needs only the previous two counts. The deeper version stores the full DP table to make that recurrence easier to see.');
  add(34,'dp[a] means the best answer currently known for amount a, so dp[a] = min(...) replaces that answer when a better one is found. We do not append because the list was already created with one fixed slot for every amount. dp[a-coin] + 1 means: solve the smaller amount a-coin, then use one additional coin.');
  add(35,'[[1] * n for _ in range(m)] creates m separate rows with n cells each. dp[r][c] refers to one fixed grid cell, so assignment updates that cell. We do not append because the whole m × n table already exists. Each cell is the number of paths from above plus the number from the left.');
  add(36,'memo is a dictionary mapping an input x to the Fibonacci value already computed for x. if x not in memo checks whether that answer is missing. memo[x] = ... stores or updates the value for that specific key; append would not work because we need direct lookup by x, not merely another item at the end of a list.');
})();