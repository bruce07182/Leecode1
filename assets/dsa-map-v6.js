// DSA map: data structures on the left, algorithms/patterns on the right.
(() => {
  // Keep Array explicit as the DSA concept; Python list is its practical implementation in these exercises.
  const DS=['Array','String','Hashing','Stack','Queue','Linked List','Heap','Tree','Graph'];
  const ALG=['Traversal','Two Pointers','Sorting','Sliding Window','Prefix Sum','Binary Search','Math','DFS / Recursion','BFS','Greedy','Backtracking','Dynamic Programming'];
  const aliases={'Traversal':'Traversal','Lists':'Array','Strings':'String','Hash Map':'Hashing','Hash Set':'Hashing','Stack':'Stack','Queue':'Queue','Linked List':'Linked List','Heap':'Heap','Graphs':'Graph','Trees / DFS':'Tree','Two Pointers':'Two Pointers','Sorting':'Sorting','Sliding Window':'Sliding Window','Prefix Sum':'Prefix Sum','Binary Search':'Binary Search','Recursion':'DFS / Recursion','1-D DP':'Dynamic Programming'};
  const related=[
    // Traversal is a general operation for structures whose elements/nodes can be visited, but the traversal method differs by structure.
    ['Array','Traversal'],['String','Traversal'],['Hashing','Traversal'],['Stack','Traversal'],['Queue','Traversal'],['Linked List','Traversal'],['Heap','Traversal'],['Tree','Traversal'],['Graph','Traversal'],
    ['Array','Two Pointers'],['Array','Sorting'],['Array','Sliding Window'],['Array','Prefix Sum'],['Array','Binary Search'],['Array','Math'],['Array','Dynamic Programming'],
    ['String','Two Pointers'],['String','Sliding Window'],['String','Math'],
    ['Hashing','Math'],['Linked List','Two Pointers'],['Heap','Greedy'],
    ['Tree','DFS / Recursion'],['Tree','BFS'],['Tree','Dynamic Programming'],
    ['Graph','BFS'],['Graph','DFS / Recursion'],['Graph','Backtracking']
  ];
  const dsTeach={
    'Array':{one:'Indexed sequence. In Python, LeetCode arrays are normally represented by list.',code:'a = [10, 20, 30]\na[1]  # 20',diagram:'[0] 10   [1] 20   [2] 30'},
    'String':{one:'An ordered sequence of characters.',code:'s = "cat"\ns[1]  # "a"',diagram:'[0] c   [1] a   [2] t'},
    'Hashing':{one:'A hash maps a key to a location for fast lookup. Set and map/dict are the two main forms.',code:'seen = {"cat", "dog"}  # set\nage = {"Amy": 20}     # map/dict',diagram:'key → hash → bucket\n              ├─ set: key\n              └─ map: key → value'},
    'Stack':{one:'Last in, first out (LIFO).',code:'s = []\ns.append(1)\ns.append(2)\ns.pop()  # 2',diagram:'TOP → 2\n      1'},
    'Queue':{one:'First in, first out (FIFO).',code:'from collections import deque\nq = deque([1, 2])\nq.popleft()  # 1',diagram:'OUT ← 1  2 ← IN'},
    'Linked List':{one:'Nodes store a value and a link to the next node.',code:'node.val\nnode.next',diagram:'[1] → [2] → [3] → None'},
    'Heap':{one:'A priority structure; Python heapq keeps the minimum easy to access/remove.',code:'import heapq\nh = [3, 1, 2]\nheapq.heapify(h)',diagram:'    1\n   / \\\n  3   2'},
    'Tree':{one:'Hierarchical nodes: a root with children.',code:'node.val\nnode.left\nnode.right',diagram:'    1\n   / \\\n  2   3'},
    'Graph':{one:'Vertices connected by edges.',code:'g = {"A": ["B", "C"]}',diagram:'A ─ B\n│\nC'}
  };
  const active=()=>new Set(qs.map((q,i)=>modeAllows(i)?aliases[q.category]:null).filter(Boolean));
  const state=name=>{const cats=[...new Set(qs.filter((q,i)=>modeAllows(i)&&aliases[q.category]===name).map(q=>q.category))];if(!cats.length)return'lockedConcept';const ids=qs.map((q,i)=>cats.includes(q.category)&&modeAllows(i)?i:-1).filter(i=>i>=0);if(ids.length&&ids.every(i=>masteryOf(i)==='Mastered'))return'mastered';if(ids.some(i=>completed(i)))return'learning';if(ids.some(i=>unlocked(i)))return'available';return'lockedConcept'};
  const safe=s=>s.replace(/[^A-Za-z0-9]/g,'_'); const oldShow=window.showConcept;
  const mini=name=>{const d=dsTeach[name];if(!d)return;let p=document.getElementById('dsMiniPopup');if(!p){p=document.createElement('div');p.id='dsMiniPopup';p.className='dsMiniPopup hidden';document.body.appendChild(p)}p.innerHTML='<div class="dsMiniInner"><button class="dsMiniClose" aria-label="Close">×</button><div class="dsTeachTitle">'+name+'</div><div class="dsTeachOne">'+d.one+'</div><div class="dsTeachGrid"><pre class="dsDiagram">'+d.diagram+'</pre><pre class="dsCode">'+d.code+'</pre></div>'+(name==='Hashing'?'<div class="status"><b>Hash Set</b> = keys only &nbsp; • &nbsp; <b>Hash Map / dict</b> = key → value</div>':'')+'</div>';p.classList.remove('hidden');p.querySelector('.dsMiniClose').onclick=()=>p.classList.add('hidden');p.onclick=e=>{if(e.target===p)p.classList.add('hidden')}};
  const showQuestions=name=>{const cats=[...new Set(qs.filter(q=>aliases[q.category]===name).map(q=>q.category))];if(cats.length===1)return oldShow(cats[0]);const items=qs.map((x,i)=>({x,i})).filter(o=>cats.includes(o.x.category)&&modeAllows(o.i));const host=document.getElementById('conceptQuestions');host.innerHTML='<div class="conceptPickHead"><b>'+name+'</b>'+(dsTeach[name]?'<button class="miniLearnBtn" type="button">Quick view</button>':'')+'</div>'+items.map(o=>'<button data-q="'+o.i+'" '+(unlocked(o.i)?'':'disabled')+'>'+(completed(o.i)?'✓ ':'')+o.x.title+(unlocked(o.i)?'':' 🔒')+'</button>').join('');const mb=host.querySelector('.miniLearnBtn');if(mb)mb.onclick=()=>mini(name);host.querySelectorAll('button[data-q]').forEach(b=>b.onclick=()=>{sel.value=b.dataset.q;load();loadCloud();['questionCard','workCard'].forEach(id=>document.getElementById(id).classList.remove('hidden'))})};
  window.showDsaConcept=showQuestions;
  window.renderConceptMap=async function(){const host=document.getElementById('conceptMap');if(!host)return;document.getElementById('mapModeLabel').textContent='Click a concept to find related questions · Quick view gives an optional DS refresher';const a=active();let ds=DS.filter(x=>a.has(x));if(a.has('Array')&&!ds.includes('Array'))ds.unshift('Array');if(a.has('Hashing')&&!ds.includes('Hashing'))ds.splice(Math.min(2,ds.length),0,'Hashing');let alg=ALG.filter(x=>a.has(x));if(mode.value==='all')['Math','BFS','Greedy','Backtracking'].forEach(x=>{if(!alg.includes(x))alg.push(x)});const all=[...ds,...alg];const lines=['flowchart LR','classDef mastered fill:#dcfce7,stroke:#70c98a,color:#172033','classDef learning fill:#fff4cc,stroke:#dfbd49,color:#172033','classDef available fill:#e8f1ff,stroke:#84aee9,color:#172033','classDef lockedConcept fill:#f1f3f5,stroke:#d8dde3,color:#7a8491'];lines.push('subgraph DS["DATA STRUCTURES · SIMPLE → COMPLEX"]','direction TB');ds.forEach(x=>lines.push('n'+safe(x)+'["'+x+'"]:::'+state(x)));lines.push('end');lines.push('subgraph AL["ALGORITHMS / PATTERNS"]','direction TB');alg.forEach(x=>lines.push('n'+safe(x)+'["'+x+'"]:::'+state(x)));lines.push('end');related.filter(([x,y])=>all.includes(x)&&all.includes(y)).forEach(([x,y])=>lines.push('n'+safe(x)+' --- n'+safe(y)));host.innerHTML='<div class="status">Drawing DSA map…</div>';if(!window.mermaid){setTimeout(renderConceptMap,150);return}try{const r=await window.mermaid.render('dsaGraph'+Date.now(),lines.join('\n'));host.innerHTML='<div class="mermaidGraph dsaGraph">'+r.svg+'</div><div class="statusline"><span class="pill mastered">Mastered</span><span class="pill learning">Learning</span><span class="pill available">Available</span><span class="pill lockedConcept">Locked</span></div>';const svg=host.querySelector('svg');all.forEach(name=>{const key='n'+safe(name);const node=svg&&[...svg.querySelectorAll('.node')].find(n=>n.id&&n.id.includes(key));if(node){node.style.cursor='pointer';node.onclick=()=>showQuestions(name)}})}catch(e){host.innerHTML='<div class="status">Could not draw DSA map. '+String(e)+'</div>'}};
  const ensureMap=()=>{const c=document.getElementById('mapCard');if(c&&!c.classList.contains('collapsed'))renderConceptMap()};setTimeout(ensureMap,350);setTimeout(ensureMap,900);
})();