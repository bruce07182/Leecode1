const SUPABASE_URL='https://eylaymrbyjjbvkgebvzv.supabase.co';
const SUPABASE_KEY='sb_publishable_SXbGa-hEOpxjEpk3bvsJEw_AByTO9I9';
const pydb=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);
const L=(level,t,tip,ex,task,start,answer,test)=>({level,t,tip,ex,task,start,answer,test});
const lessons=[
L(0,'Variables','<code>=</code> stores a value. Python infers its type.','x = 7              # int\nname = "Amy"       # str\nready = True       # bool\nprint(x)','Set <code>x = 7</code> and print it.','# Try it\n','x = 7\nprint(x)',o=>o.trim()==='7'),
L(0,'if / else','<code>% 2</code> gives the remainder after division by 2.','x = 8\nif x % 2 == 0:     # == compares\n    print("even")\nelse:\n    print("odd")','Set <code>x = 7</code>. Print <code>even</code> or <code>odd</code>.','x = 7\n','x = 7\nif x % 2 == 0:\n    print("even")\nelse:\n    print("odd")',o=>o.trim()==='odd'),
L(0,'for loop','For a list, this is the basic form of <b>traversal</b>.','nums = [2, 4, 6]\nfor x in nums:      # x = each value\n    print(x)','Print every value in <code>[3,5,7]</code>.','nums = [3, 5, 7]\n','nums = [3, 5, 7]\nfor x in nums:\n    print(x)',o=>o.trim()==='3\n5\n7'),
L(0,'Lists','Python <code>list</code> is the array-like structure used in most DSA problems.','a = [10, 20]\nprint(a[0])          # 10, index starts at 0\na.append(30)         # [10, 20, 30]\nprint(len(a))        # 3','Append <code>3</code> to <code>[1,2]</code>, then print the list.','a = [1, 2]\n','a = [1, 2]\na.append(3)\nprint(a)',o=>o.trim()==='[1, 2, 3]'),
L(0,'Functions','<code>return</code> gives the result back. LeetCode solutions are usually functions/methods.','def double(x):\n    return x * 2\n\nanswer = double(4)\nprint(answer)        # 8','Complete <code>square(x)</code>.','def square(x):\n    pass\n\nprint(square(5))','def square(x):\n    return x * x\n\nprint(square(5))',o=>o.trim()==='25'),

L(1,'Values & None','These are the common value types worth recognizing.','count = 3           # int\nratio = 0.5         # float\nname = "Amy"       # str\nfound = False       # bool\nnode = None         # no value yet','Create <code>found = False</code> and print it.','','found = False\nprint(found)',o=>o.trim()==='False'),
L(1,'Conditions','Use comparisons with <code>and</code>, <code>or</code>, <code>not</code>.','x = 6\nif x > 0 and x % 2 == 0:\n    print("positive even")\n\n# == != < > <= >=','Print <code>yes</code> if <code>x=6</code> is between 1 and 10.','x = 6\n','x = 6\nif x >= 1 and x <= 10:\n    print("yes")',o=>o.trim()==='yes'),
L(1,'Indexes & range','Use indexes only when the position matters.','a = [10, 20, 30]\nfor i in range(len(a)):  # i = 0,1,2\n    print(i, a[i])','Print indexes 0, 1, 2 for the list.','a = [5, 6, 7]\n','a = [5, 6, 7]\nfor i in range(len(a)):\n    print(i)',o=>o.trim()==='0\n1\n2'),
L(1,'while loop','Useful when you do not simply visit every value once.','i = 0\nwhile i < 3:\n    print(i)\n    i += 1          # i = i + 1','Use <code>while</code> to print 1, 2, 3.','i = 1\n','i = 1\nwhile i <= 3:\n    print(i)\n    i += 1',o=>o.trim()==='1\n2\n3'),
L(1,'List operations','These are the list operations used most often in DSA.','a = [10, 20, 30]\na[0]                 # first\na[-1]                # last\na[1:3]               # [20, 30]\na.append(40)         # add at end\nx = a.pop()          # remove/return last\n20 in a              # membership','Append 4, pop it, then print the popped value.','a = [1, 2, 3]\n','a = [1, 2, 3]\na.append(4)\nx = a.pop()\nprint(x)',o=>o.trim()==='4'),
L(1,'Strings','A string is sequence-like, but cannot be changed in place.','s = "hello"\ns[0]                  # "h"\ns[-1]                 # "o"\ns[::-1]               # "olleh"\nwords = "a b".split() # ["a", "b"]\n"-".join(words)       # "a-b"','Print <code>hello</code> reversed.','s = "hello"\n','s = "hello"\nprint(s[::-1])',o=>o.trim()==='olleh'),
L(1,'Dictionary','Use a dictionary for key → value lookup and counting.','counts = {}\nfor x in [1, 1, 2]:\n    counts[x] = counts.get(x, 0) + 1\n# get(x, 0): use 0 when x is missing\nprint(counts)         # {1: 2, 2: 1}','Count <code>[1,1,2]</code> and print the dictionary.','nums = [1, 1, 2]\ncounts = {}\n','nums = [1, 1, 2]\ncounts = {}\nfor x in nums:\n    counts[x] = counts.get(x, 0) + 1\nprint(counts)',o=>o.replace(/\s/g,'')==='{1:2,2:1}'),
L(1,'Set','Use a set when you only need unique values / fast membership.','seen = set()\nseen.add(5)\nprint(5 in seen)     # True\n\n# common DSA use: duplicates / visited','Add 3 and print whether it is in the set.','seen = set()\n','seen = set()\nseen.add(3)\nprint(3 in seen)',o=>o.trim()==='True'),
L(1,'Unpacking','Very common for pointers, pairs, swaps, and loops.','left, right = 0, 5\nleft, right = right, left   # swap\n\nfor key, value in {"a": 1}.items():\n    print(key, value)','Swap <code>a=1</code> and <code>b=2</code> in one line.','a, b = 1, 2\n','a, b = 1, 2\na, b = b, a\nprint(a, b)',o=>o.trim()==='2 1'),
L(1,'Built-ins','Know these; do not rewrite what Python already provides.','a = [3, 1, 2]\nlen(a)                # 3\nmin(a), max(a)        # 1, 3\nsum(a)                # 6\nsorted(a)             # [1, 2, 3]','Print the sum of <code>[3,4,5]</code>.','a = [3, 4, 5]\n','a = [3, 4, 5]\nprint(sum(a))',o=>o.trim()==='12'),

L(2,'enumerate & zip','Cleaner forms for indexed and paired traversal.','a = [10, 20]\nfor i, x in enumerate(a):\n    print(i, x)       # index + value\n\na = [1, 2]; b = [3, 4]\nfor x, y in zip(a, b):\n    print(x, y)       # paired values','Use <code>enumerate</code> to print <code>0 a</code>, <code>1 b</code>.','a = ["a", "b"]\n','a = ["a", "b"]\nfor i, x in enumerate(a):\n    print(i, x)',o=>o.trim()==='0 a\n1 b'),
L(2,'Sorting','Use <code>key=</code> when sorting by part of an item.','pairs = [[1, 5], [2, 1], [3, 3]]\npairs.sort(key=lambda p: p[1])\n# [[2,1], [3,3], [1,5]]\n\n# sorted(a) -> new list\n# a.sort()  -> changes a','Sort the pairs by their second value and print them.','pairs = [[1,5],[2,1],[3,3]]\n','pairs = [[1,5],[2,1],[3,3]]\npairs.sort(key=lambda p: p[1])\nprint(pairs)',o=>o.replace(/\s/g,'')==='[[2,1],[3,3],[1,5]]'),
L(2,'Comprehensions','Use for short, obvious transform/filter operations.','nums = [1, 2, 3, 4]\nsquares = [x*x for x in nums]\nevens = [x for x in nums if x % 2 == 0]\n# prefer a normal loop if this gets hard to read','Build and print the even values with a comprehension.','nums = [1, 2, 3, 4]\n','nums = [1, 2, 3, 4]\nprint([x for x in nums if x % 2 == 0])',o=>o.trim()==='[2, 4]'),
L(2,'deque','Use as a queue when removing from the front.','from collections import deque\nq = deque([1, 2])\nq.append(3)           # add right\nfirst = q.popleft()   # remove left, O(1)\n\n# list.pop(0) is O(n)','Pop the leftmost value from <code>[1,2,3]</code>.','from collections import deque\n','from collections import deque\nq = deque([1,2,3])\nprint(q.popleft())',o=>o.trim()==='1'),
L(2,'Counter & defaultdict','Useful hash helpers for counting and grouping.','from collections import Counter, defaultdict\n\nc = Counter("aab")\nprint(c["a"])        # 2\n\ngraph = defaultdict(list)\ngraph[1].append(2)    # missing key starts as []','Use <code>Counter</code> to count the a characters in <code>aab</code>.','from collections import Counter\n','from collections import Counter\nc = Counter("aab")\nprint(c["a"])',o=>o.trim()==='2'),
L(2,'heapq','Python heap = min-heap. Use for repeated smallest/largest-priority work.','import heapq\nh = []\nheapq.heappush(h, 5)\nheapq.heappush(h, 2)\nprint(heapq.heappop(h))  # 2','Push 5, 1, 3 and pop the smallest.','import heapq\nh = []\n','import heapq\nh = []\nfor x in [5,1,3]:\n    heapq.heappush(h, x)\nprint(heapq.heappop(h))',o=>o.trim()==='1'),
L(2,'Useful idioms','Only a few concise forms are worth knowing early.','best = float("inf")       # very large start\na, b = b, a              # swap\nexists = x in seen        # membership\nvalue = x if ok else y    # conditional value\n\nany(flags)\nall(flags)','Set <code>best</code> to infinity; print whether 999999 is smaller.','','best = float("inf")\nprint(999999 < best)',o=>o.trim()==='True')
];
const levelInfo={0:'Essentials · 5 topics · learn enough Python to start coding.',1:'Core · everyday Python used repeatedly in DSA.',2:'DSA Python · useful tools and idioms for algorithm problems.'};
let level=+(localStorage.getItem('py_level')||0),current=0,pyodide=null;
const $=id=>document.getElementById(id),esc=s=>String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
const visible=()=>lessons.filter(x=>x.level===level);
function renderTopics(){const a=visible();$('pyTopics').innerHTML=a.map((x,i)=>`<button data-i="${i}" class="${i===current?'active':''}">${i+1}. ${x.t}</button>`).join('');$('pyTopics').querySelectorAll('button').forEach(b=>b.onclick=()=>{current=+b.dataset.i;render()});document.querySelectorAll('#levelBar button').forEach(b=>b.classList.toggle('active',+b.dataset.level===level));$('levelDesc').textContent=levelInfo[level];}
function render(){const x=visible()[current]||visible()[0];if(!x)return;renderTopics();$('pyLesson').innerHTML=`<h2>${x.t}</h2>${x.tip?`<div class="why">${x.tip}</div>`:''}<pre class="pyExample">${esc(x.ex)}</pre>`;$('pyTask').innerHTML=x.task;$('pyCode').value=localStorage.getItem(`py_code_${level}_${current}`)||x.start;$('pyOut').textContent=pyodide?'Ready.':'Python is loading…';$('pyAnswerBox').classList.remove('show');$('pyAnswerBox').innerHTML='';}
document.querySelectorAll('#levelBar button').forEach(b=>b.onclick=()=>{level=+b.dataset.level;localStorage.setItem('py_level',level);current=0;render()});
async function initPython(){try{pyodide=await loadPyodide();$('pyOut').textContent='Ready.'}catch(e){$('pyOut').textContent='Could not load Python: '+e}}
$('pyCode').addEventListener('input',()=>localStorage.setItem(`py_code_${level}_${current}`,$('pyCode').value));
$('pyCode').addEventListener('keydown',e=>{const ta=e.currentTarget;if(e.key==='Tab'){e.preventDefault();const s=ta.selectionStart,en=ta.selectionEnd;if(e.shiftKey){const before=ta.value.slice(0,s),line=before.lastIndexOf('\n')+1;const n=ta.value.slice(line,line+4).match(/^ {1,4}/)?.[0].length||0;ta.value=ta.value.slice(0,line)+ta.value.slice(line+n);ta.selectionStart=ta.selectionEnd=Math.max(line,s-n)}else{ta.setRangeText('    ',s,en,'end')}ta.dispatchEvent(new Event('input'))}else if(e.key==='Enter'){const before=ta.value.slice(0,ta.selectionStart),line=before.slice(before.lastIndexOf('\n')+1),indent=(line.match(/^\s*/)||[''])[0]+(line.trimEnd().endsWith(':')?'    ':'');if(indent){e.preventDefault();ta.setRangeText('\n'+indent,ta.selectionStart,ta.selectionEnd,'end');ta.dispatchEvent(new Event('input'))}}});
$('pyRun').onclick=async()=>{if(!pyodide)return;const x=visible()[current];$('pyOut').textContent='Running…';try{pyodide.runPython('import sys, io\n_buf=io.StringIO()\nsys.stdout=_buf');await pyodide.runPythonAsync($('pyCode').value);const out=pyodide.runPython('_buf.getvalue()');pyodide.runPython('sys.stdout=sys.__stdout__');$('pyOut').textContent=(x.test(out)?'✓ Passed\n':'Not yet\n')+(out||'(no output)')}catch(e){try{pyodide.runPython('sys.stdout=sys.__stdout__')}catch(_){}$('pyOut').textContent=String(e)}};
$('pyReset').onclick=()=>{localStorage.removeItem(`py_code_${level}_${current}`);render()};
$('pyAnswer').onclick=()=>{const box=$('pyAnswerBox'),x=visible()[current];if(box.classList.contains('show')){box.classList.remove('show');box.innerHTML='';return}box.innerHTML='<b>Answer</b><pre>'+esc(x.answer)+'</pre>';box.classList.add('show')};
$('pyLogin').onclick=async()=>{const {error}=await pydb.auth.signInWithPassword({email:$('pyEmail').value.trim(),password:$('pyPassword').value});$('pyAuthMsg').textContent=error?error.message:''};
function gate(user){document.body.classList.toggle('auth-locked',!user);$('pyAuth').classList.toggle('hidden',!!user)}
pydb.auth.getSession().then(({data})=>gate(data.session?.user||null));pydb.auth.onAuthStateChange((e,s)=>gate(s?.user||null));
render();initPython();