const SUPABASE_URL='https://eylaymrbyjjbvkgebvzv.supabase.co';
const SUPABASE_KEY='sb_publishable_SXbGa-hEOpxjEpk3bvsJEw_AByTO9I9';
const pydb=supabase.createClient(SUPABASE_URL,SUPABASE_KEY);
const L=(level,t,why,learn,ex,task,start,answer,test)=>({level,t,why,learn,ex,task,start,answer,test});
const lessons=[
L(0,'Variables & values','Remember a value with a name.','Start with only the values you use constantly: numbers, text, and <code>True</code>/<code>False</code>. Assignment uses <code>=</code>.','x = 7\nname = "Amy"\nready = True','Set <code>x = 7</code> and print it.','# Try it\n','x = 7\nprint(x)',o=>o.trim()==='7'),
L(0,'if / else','Run different code depending on a condition.','Use <code>if</code> for a test and <code>else</code> for the other case. Indentation tells Python which lines belong inside each part.','x = 8\nif x > 0:\n    print("positive")\nelse:\n    print("not positive")','Set <code>x = 8</code>. Print <code>even</code> when it is even, otherwise <code>odd</code>.','x = 8\n','x = 8\nif x % 2 == 0:\n    print("even")\nelse:\n    print("odd")',o=>o.trim()==='even'),
L(0,'for loop','Repeat the same action for each value.','A <code>for</code> loop is the normal way to traverse a Python list. The variable after <code>for</code> becomes each value in turn.','nums = [2, 4, 6]\nfor x in nums:\n    print(x)','Print every value in <code>[2,4,6]</code>, one per line.','nums = [2, 4, 6]\n','nums = [2, 4, 6]\nfor x in nums:\n    print(x)',o=>o.trim()==='2\n4\n6'),
L(0,'Lists','Keep several values in order.','For DSA, Python lists are the everyday array-like structure. Read with <code>a[i]</code>, add with <code>append</code>, and get size with <code>len(a)</code>.','a = [10, 20]\nprint(a[0])\na.append(30)','Start with <code>[1,2]</code>, append <code>3</code>, then print the list.','a = [1, 2]\n','a = [1, 2]\na.append(3)\nprint(a)',o=>o.trim()==='[1, 2, 3]'),
L(0,'Functions','Give a piece of code an input and get an answer back.','LeetCode mostly asks you to complete functions. <code>return</code> sends the answer back; <code>print</code> only displays it.','def double(x):\n    return x * 2\n\nprint(double(4))','Complete <code>square(x)</code> and print <code>square(5)</code>.','def square(x):\n    pass\n\nprint(square(5))','def square(x):\n    return x * x\n\nprint(square(5))',o=>o.trim()==='25'),

L(1,'Types & None','Recognize the common Python values you will see in solutions.','Know <code>int</code>, <code>float</code>, <code>str</code>, <code>bool</code>, and <code>None</code>. Usually you do not declare types before assigning values.','count = 3\nratio = 0.5\nname = "Amy"\nfound = False\nnode = None','Create <code>found = False</code> and print it.','', 'found = False\nprint(found)',o=>o.trim()==='False'),
L(1,'Conditions','Combine comparisons clearly.','Common comparisons are <code>== != &lt; &gt; &lt;= &gt;=</code>. Combine them with <code>and</code>, <code>or</code>, and <code>not</code>.','x = 7\nif x > 0 and x < 10:\n    print("single digit")','Print <code>yes</code> if <code>x=6</code> is positive and even.','x = 6\n','x = 6\nif x > 0 and x % 2 == 0:\n    print("yes")',o=>o.trim()==='yes'),
L(1,'range & indexes','Loop over positions when you need indexes, not just values.','<code>range(n)</code> produces 0 through n-1. Use <code>range(len(a))</code> when the index itself matters.','a = [10, 20, 30]\nfor i in range(len(a)):\n    print(i, a[i])','Print the indexes <code>0</code>, <code>1</code>, <code>2</code> for the given list.','a = [5, 6, 7]\n','a = [5, 6, 7]\nfor i in range(len(a)):\n    print(i)',o=>o.trim()==='0\n1\n2'),
L(1,'while loop','Repeat while a condition remains true.','Use <code>while</code> when movement depends on a condition. This becomes important for two pointers, binary search, and linked lists.','i = 0\nwhile i < 3:\n    print(i)\n    i += 1','Use <code>while</code> to print 1, 2, 3.','i = 1\n','i = 1\nwhile i <= 3:\n    print(i)\n    i += 1',o=>o.trim()==='1\n2\n3'),
L(1,'List operations','Use the list operations that appear constantly in coding problems.','Know indexing, negative indexing, slicing, <code>append</code>, <code>pop</code>, <code>len</code>, and <code>in</code>.','a = [10,20,30]\nlast = a[-1]\na.append(40)\nx = a.pop()','Append 4 to <code>[1,2,3]</code>, pop it back off, then print the popped value.','a = [1, 2, 3]\n','a = [1, 2, 3]\na.append(4)\nx = a.pop()\nprint(x)',o=>o.trim()==='4'),
L(1,'Strings','Treat text as a sequence of characters.','Strings support indexing and slicing like lists but are immutable. Frequently useful operations include <code>split</code> and <code>join</code>.','s = "hello"\nprint(s[0])\nprint(s[::-1])','Print <code>hello</code> reversed using slicing.','s = "hello"\n','s = "hello"\nprint(s[::-1])',o=>o.trim()==='olleh'),
L(1,'Dictionary','Map a key to a value.','Use <code>d[key]</code> when a key must exist. <code>d.get(key, default)</code> is useful when it may be missing.','counts = {}\ncounts["a"] = counts.get("a", 0) + 1','Count values in <code>[1,1,2]</code> and print the dictionary.','nums = [1, 1, 2]\ncounts = {}\n','nums = [1, 1, 2]\ncounts = {}\nfor x in nums:\n    counts[x] = counts.get(x, 0) + 1\nprint(counts)',o=>o.replace(/\s/g,'')==='{1:2,2:1}'),
L(1,'Set','Remember unique values and test membership quickly.','Use <code>set()</code>, <code>add</code>, and <code>x in seen</code>. Sets are central to duplicate and visited-state problems.','seen = set()\nseen.add(5)\nprint(5 in seen)','Add 3 to a set and print whether 3 is present.','seen = set()\n','seen = set()\nseen.add(3)\nprint(3 in seen)',o=>o.trim()==='True'),
L(1,'Tuple & unpacking','Assign or return several values compactly.','Unpacking is common in pointer code, coordinates, dictionary iteration, and swaps.','left, right = 0, 5\nleft, right = right, left','Swap <code>a=1</code> and <code>b=2</code> in one line, then print them.','a, b = 1, 2\n','a, b = 1, 2\na, b = b, a\nprint(a, b)',o=>o.trim()==='2 1'),
L(1,'Useful built-ins','Use Python tools instead of rewriting routine work.','Prioritize <code>len</code>, <code>min</code>, <code>max</code>, <code>sum</code>, <code>sorted</code>, <code>enumerate</code>, and <code>zip</code>.','a = [3,1,2]\nprint(max(a))\nprint(sorted(a))','Print the sum of <code>[3,4,5]</code> using a built-in.','a = [3, 4, 5]\n','a = [3, 4, 5]\nprint(sum(a))',o=>o.trim()==='12'),

L(2,'enumerate & zip','Write common indexed and paired traversals cleanly.','<code>enumerate(a)</code> gives index + value. <code>zip(a,b)</code> traverses sequences together.','for i, x in enumerate(a):\n    print(i, x)\nfor x, y in zip(a, b):\n    print(x, y)','Use <code>enumerate</code> to print <code>0 a</code> then <code>1 b</code>.','a = ["a", "b"]\n','a = ["a", "b"]\nfor i, x in enumerate(a):\n    print(i, x)',o=>o.trim()==='0 a\n1 b'),
L(2,'Sorting with key','Control what Python sorts by.','Use <code>sorted</code> for a new list and <code>.sort()</code> to modify a list. <code>key=</code> selects the value used for comparison.','pairs = [["a",3],["b",1]]\npairs = sorted(pairs, key=lambda p: p[1])','Sort <code>[[1,5],[2,1],[3,3]]</code> by the second value and print it.','pairs = [[1,5],[2,1],[3,3]]\n','pairs = [[1,5],[2,1],[3,3]]\nprint(sorted(pairs, key=lambda p: p[1]))',o=>o.replace(/\s/g,'')==='[[2,1],[3,3],[1,5]]'),
L(2,'Comprehensions','Build simple transformed or filtered collections compactly.','A comprehension is useful when the operation stays obvious. Prefer a normal loop when logic becomes harder to read.','squares = [x*x for x in nums]\nevens = [x for x in nums if x%2 == 0]','Build and print the even values from <code>[1,2,3,4]</code> with a list comprehension.','nums = [1, 2, 3, 4]\n','nums = [1, 2, 3, 4]\nprint([x for x in nums if x % 2 == 0])',o=>o.trim()==='[2, 4]'),
L(2,'deque','Use an efficient queue or double-ended queue.','A list is good as a stack, but removing from its front is O(n). <code>collections.deque</code> gives O(1) operations at both ends.','from collections import deque\nq = deque([1,2])\nq.append(3)\nfirst = q.popleft()','Create a deque [1,2,3], remove the leftmost value, and print it.','from collections import deque\n','from collections import deque\nq = deque([1,2,3])\nprint(q.popleft())',o=>o.trim()==='1'),
L(2,'Counter & defaultdict','Use standard hash-based helpers when they make intent clearer.','<code>Counter</code> builds frequencies. <code>defaultdict</code> creates a default value for a missing key—useful for grouping and graphs.','from collections import Counter, defaultdict\nc = Counter("aab")\ng = defaultdict(list)\ng[1].append(2)','Use <code>Counter</code> to count <code>"aab"</code> and print the count of a.','from collections import Counter\n','from collections import Counter\nc = Counter("aab")\nprint(c["a"])',o=>o.trim()==='2'),
L(2,'heapq','Use Python’s built-in min-heap for priority problems.','<code>heapq</code> operates on a list. <code>heappush</code> adds and <code>heappop</code> removes the smallest value.','import heapq\nh = []\nheapq.heappush(h, 5)\nheapq.heappush(h, 2)\nprint(heapq.heappop(h))','Push 5, 1, 3 into a heap and print the smallest by popping once.','import heapq\nh = []\n','import heapq\nh = []\nfor x in [5,1,3]:\n    heapq.heappush(h, x)\nprint(heapq.heappop(h))',o=>o.trim()==='1'),
L(2,'Useful idioms','Recognize concise Python commonly seen in strong LeetCode solutions.','Useful idioms include multiple assignment, infinity, membership, <code>any/all</code>, and conditional expressions. Learn them for readability, not cleverness.','best = float("inf")\na, b = b, a\nvalue = x if ok else y','Set <code>best</code> to positive infinity and print whether 999999 is smaller.','','best = float("inf")\nprint(999999 < best)',o=>o.trim()==='True')
];
const levelInfo={0:'Absolute essentials · only 5 topics. Enough Python to start understanding very simple DSA code.',1:'Core Python · the everyday language features used repeatedly in LeetCode.',2:'DSA Python · deeper Python tools and idioms that make algorithm implementations cleaner and faster.'};
let level=+(localStorage.getItem('py_level')||0),current=0,pyodide=null;
const $=id=>document.getElementById(id),esc=s=>String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
const visible=()=>lessons.filter(x=>x.level===level);
function renderTopics(){const a=visible();$('pyTopics').innerHTML=a.map((x,i)=>`<button data-i="${i}" class="${i===current?'active':''}">${i+1}. ${x.t}</button>`).join('');$('pyTopics').querySelectorAll('button').forEach(b=>b.onclick=()=>{current=+b.dataset.i;render()});document.querySelectorAll('#levelBar button').forEach(b=>b.classList.toggle('active',+b.dataset.level===level));$('levelDesc').textContent=levelInfo[level];}
function render(){const x=visible()[current]||visible()[0];if(!x)return;renderTopics();$('pyLesson').innerHTML=`<h2>${x.t}</h2><div class="why">${x.why}</div><div>${x.learn}</div><pre class="pyExample">${esc(x.ex)}</pre>`;$('pyTask').innerHTML=x.task;$('pyCode').value=localStorage.getItem(`py_code_${level}_${current}`)||x.start;$('pyOut').textContent=pyodide?'Ready.':'Python is loading…';$('pyAnswerBox').classList.remove('show');$('pyAnswerBox').innerHTML='';}
document.querySelectorAll('#levelBar button').forEach(b=>b.onclick=()=>{level=+b.dataset.level;localStorage.setItem('py_level',level);current=0;render()});
async function initPython(){try{pyodide=await loadPyodide();$('pyOut').textContent='Ready.'}catch(e){$('pyOut').textContent='Could not load Python: '+e}}
$('pyCode').addEventListener('input',()=>localStorage.setItem(`py_code_${level}_${current}`,$('pyCode').value));
$('pyCode').addEventListener('keydown',e=>{const ta=e.currentTarget;if(e.key==='Tab'){e.preventDefault();const s=ta.selectionStart,en=ta.selectionEnd;if(e.shiftKey){const before=ta.value.slice(0,s),line=before.lastIndexOf('\n')+1;const n=ta.value.slice(line,line+4).match(/^ {1,4}/)?.[0].length||0;ta.value=ta.value.slice(0,line)+ta.value.slice(line+n);ta.selectionStart=ta.selectionEnd=Math.max(line,s-n)}else{ta.setRangeText('    ',s,en,'end')}ta.dispatchEvent(new Event('input'))}else if(e.key==='Enter'){const before=ta.value.slice(0,ta.selectionStart),line=before.slice(before.lastIndexOf('\n')+1),indent=(line.match(/^\s*/)||[''])[0]+(line.trimEnd().endsWith(':')?'    ':'');if(indent){e.preventDefault();ta.setRangeText('\n'+indent,ta.selectionStart,ta.selectionEnd,'end');ta.dispatchEvent(new Event('input'))}}});
$('pyRun').onclick=async()=>{if(!pyodide)return;const x=visible()[current];$('pyOut').textContent='Running…';try{pyodide.runPython('import sys, io\n_buf=io.StringIO()\nsys.stdout=_buf');await pyodide.runPythonAsync($('pyCode').value);const out=pyodide.runPython('_buf.getvalue()');pyodide.runPython('sys.stdout=sys.__stdout__');$('pyOut').textContent=(x.test(out)?'✓ Passed\n':'Not yet\n')+(out||'(no output)')}catch(e){try{pyodide.runPython('sys.stdout=sys.__stdout__')}catch(_){}$('pyOut').textContent=String(e)}};
$('pyReset').onclick=()=>{localStorage.removeItem(`py_code_${level}_${current}`);render()};
$('pyAnswer').onclick=()=>{const box=$('pyAnswerBox'),x=visible()[current];if(box.classList.contains('show')){box.classList.remove('show');box.innerHTML='';return}box.innerHTML='<b>Answer</b><pre>'+esc(x.answer)+'</pre>';box.classList.add('show')};
$('pyLogin').onclick=async()=>{const {error}=await pydb.auth.signInWithPassword({email:$('pyEmail').value.trim(),password:$('pyPassword').value});$('pyAuthMsg').textContent=error?error.message:''};
function gate(user){document.body.classList.toggle('auth-locked',!user);$('pyAuth').classList.toggle('hidden',!!user)}
pydb.auth.getSession().then(({data})=>gate(data.session?.user||null));pydb.auth.onAuthStateChange((e,s)=>gate(s?.user||null));
render();initPython();